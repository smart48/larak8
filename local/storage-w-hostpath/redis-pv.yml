apiVersion: v1
kind: PersistentVolume
metadata:
  name: reids-storage
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: standard
  hostPath:
  # hostPath is on VM so inside minikube
      path: /data/redis/